import 'package:flutter/material.dart';

class bio_hall extends StatefulWidget {
  bio_hall({Key? key}) : super(key: key);

  @override
  _bio_hallState createState() => _bio_hallState();
}

class _bio_hallState extends State<bio_hall> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
